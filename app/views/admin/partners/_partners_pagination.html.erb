<div id="partners_pagination">
  <% if next_page.present? %>
    <div class="flex justify-center px-3 py-4">
      <div
        class="h-6 w-full"
        data-controller="infinite-scroll"
        data-infinite-scroll-url-value="<%= admin_partners_path(q: query, page: next_page, format: :turbo_stream) %>"
      ></div>
    </div>
  <% end %>
</div>
